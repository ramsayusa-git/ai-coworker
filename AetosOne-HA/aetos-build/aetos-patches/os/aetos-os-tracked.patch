diff --git a/buildroot-external/configs/generic_x86_64_defconfig b/buildroot-external/configs/generic_x86_64_defconfig
index bf2e5bf8..9081f159 100644
--- a/buildroot-external/configs/generic_x86_64_defconfig
+++ b/buildroot-external/configs/generic_x86_64_defconfig
@@ -7,8 +7,8 @@ BR2_CCACHE_DIR="/cache/cc"
 BR2_ENABLE_LTO=y
 BR2_GLOBAL_PATCH_DIR="$(BR2_EXTERNAL_HAOS_PATH)/patches"
 BR2_SSP_REGULAR=y
-BR2_TARGET_GENERIC_HOSTNAME="homeassistant"
-BR2_TARGET_GENERIC_ISSUE="Welcome to Home Assistant"
+BR2_TARGET_GENERIC_HOSTNAME="aetosone"
+BR2_TARGET_GENERIC_ISSUE="Welcome to Aetos One"
 BR2_INIT_SYSTEMD=y
 # BR2_TARGET_GENERIC_REMOUNT_ROOTFS_RW is not set
 BR2_TARGET_LOCALTIME=""
diff --git a/buildroot-external/configs/rpi4_64_defconfig b/buildroot-external/configs/rpi4_64_defconfig
index cf9aba5f..668df02c 100644
--- a/buildroot-external/configs/rpi4_64_defconfig
+++ b/buildroot-external/configs/rpi4_64_defconfig
@@ -8,8 +8,8 @@ BR2_CCACHE_DIR="/cache/cc"
 BR2_ENABLE_LTO=y
 BR2_GLOBAL_PATCH_DIR="$(BR2_EXTERNAL_HAOS_PATH)/board/raspberrypi/patches $(BR2_EXTERNAL_HAOS_PATH)/patches"
 BR2_SSP_REGULAR=y
-BR2_TARGET_GENERIC_HOSTNAME="homeassistant"
-BR2_TARGET_GENERIC_ISSUE="Welcome to Home Assistant"
+BR2_TARGET_GENERIC_HOSTNAME="aetosone"
+BR2_TARGET_GENERIC_ISSUE="Welcome to Aetos One"
 BR2_INIT_SYSTEMD=y
 # BR2_TARGET_GENERIC_REMOUNT_ROOTFS_RW is not set
 BR2_TARGET_LOCALTIME=""
diff --git a/buildroot-external/meta b/buildroot-external/meta
index d8c3a15a..ea83f9b9 100644
--- a/buildroot-external/meta
+++ b/buildroot-external/meta
@@ -2,7 +2,7 @@ VERSION_MAJOR="18"
 VERSION_MINOR="2"
 VERSION_SUFFIX="dev0"
 
-HAOS_NAME="Home Assistant OS"
+HAOS_NAME="Aetos One OS"
 HAOS_ID="haos"
 
 DEPLOYMENT="development"
diff --git a/buildroot-external/package/hassio/create-data-partition.sh b/buildroot-external/package/hassio/create-data-partition.sh
index bc717c17..5f3e5fe3 100755
--- a/buildroot-external/package/hassio/create-data-partition.sh
+++ b/buildroot-external/package/hassio/create-data-partition.sh
@@ -12,8 +12,11 @@ data_dir="${build_dir}/data"
 APPARMOR_URL="https://version.home-assistant.io/apparmor_${channel}.txt"
 
 # Make image
+# 6G working size (pre-warm removed in v9.8.8); resize2fs -M
+# below shrinks the fs back to its true minimum so the FINAL image only grows by
+# the real unpacked content.
 rm -f "${data_img}"
-truncate --size="1280M" "${data_img}"
+truncate --size="12288M" "${data_img}"
 mkfs.ext4 -L "hassos-data" -E lazy_itable_init=0,lazy_journal_init=0 "${data_img}"
 
 # Mount / init file structs
@@ -35,6 +38,11 @@ sudo bash -ex <<EOF
 # Indicator for docker-prepare.service to use the containerd snapshotter
 touch "${data_dir}/.docker-use-containerd-snapshotter"
 
+# AETOS ONE: seed HA config only (v7.3 = plain HTTP on 8123, no SSL/cert baked)
+mkdir -p "${data_dir}/supervisor/homeassistant"
+tar xf /build/buildroot-external/package/hassio/aetos-seed.tar -C "${data_dir}/supervisor/homeassistant"
+
+
 # Setup AppArmor
 mkdir -p "${data_dir}/supervisor/apparmor"
 curl -fsL -o "${data_dir}/supervisor/apparmor/hassio-supervisor" "${APPARMOR_URL}"
diff --git a/buildroot-external/package/hassio/dind-import-containers.sh b/buildroot-external/package/hassio/dind-import-containers.sh
index d3397b08..88bd95d8 100755
--- a/buildroot-external/package/hassio/dind-import-containers.sh
+++ b/buildroot-external/package/hassio/dind-import-containers.sh
@@ -7,11 +7,8 @@ while ! docker version 2> /dev/null > /dev/null; do
 	sleep 1
 done
 
-# Install Supervisor, plug-ins and landing page
+# Install Supervisor, plug-ins, branded Core (largest first - docker load needs space)
 echo "Loading container images..."
-
-# Make sure to order images by size (largest first)
-# It seems docker load requires space during operation
 # shellcheck disable=SC2045
 for image in $(ls -S /build/images/*.tar); do
 	docker load --input "${image}"
@@ -21,3 +18,6 @@ done
 supervisor=$(docker images --filter "label=io.hass.type=supervisor" --quiet)
 arch=$(docker inspect --format '{{ index .Config.Labels "io.hass.arch" }}' "${supervisor}")
 docker tag "${supervisor}" "ghcr.io/home-assistant/${arch}-hassio-supervisor:latest"
+
+# AETOS ONE v9.8.8: pre-warm REMOVED (smaller ~1GB image; Core unpacks on first boot).
+echo "Container images loaded (pre-warm disabled)."
diff --git a/buildroot-external/package/hassio/hassio.mk b/buildroot-external/package/hassio/hassio.mk
index 4f73167e..84912628 100644
--- a/buildroot-external/package/hassio/hassio.mk
+++ b/buildroot-external/package/hassio/hassio.mk
@@ -18,11 +18,11 @@ else ifeq ($(BR2_PACKAGE_HASSIO_CHANNEL_DEV),y)
 HASSIO_VERSION_CHANNEL = "dev"
 endif
 
-HASSIO_CONTAINER_IMAGES_ARCH = supervisor dns audio cli multicast observer core
+HASSIO_CONTAINER_IMAGES_ARCH = supervisor dns audio cli multicast observer
 
 define HASSIO_CONFIGURE_CMDS
-	# Deploy only landing page for "core" by setting version to "landingpage"
-	curl -s $(HASSIO_VERSION_URL)$(HASSIO_VERSION_CHANNEL)".json" | jq '.core = "landingpage"' > $(@D)/version.json
+	# AETOS ONE: pin core to our preloaded branded core version (offline, no pull)
+	curl -s $(HASSIO_VERSION_URL)$(HASSIO_VERSION_CHANNEL)".json" | jq '.core = "2026.8.0"' > $(@D)/version.json
 endef
 
 define HASSIO_BUILD_CMDS
@@ -32,6 +32,8 @@ define HASSIO_BUILD_CMDS
 		$(BR2_EXTERNAL_HAOS_PATH)/package/hassio/fetch-container-image.sh \
 			$(BR2_PACKAGE_HASSIO_ARCH) $(BR2_PACKAGE_HASSIO_MACHINE) $(@D)/version.json $(image) "$(HASSIO_DL_DIR)" "$(@D)/images"
 	)
+	# AETOS ONE: preload branded core image (baked frontend) for offline first boot
+	$(Q)cp $(BR2_EXTERNAL_HAOS_PATH)/package/hassio/aetos-core.tar $(@D)/images/
 endef
 
 HASSIO_INSTALL_IMAGES = YES
diff --git a/buildroot-external/rootfs-overlay/etc/motd b/buildroot-external/rootfs-overlay/etc/motd
index 0f3181e6..5be50a05 100644
--- a/buildroot-external/rootfs-overlay/etc/motd
+++ b/buildroot-external/rootfs-overlay/etc/motd
@@ -1,3 +1,12 @@
-Welcome to [01;34mHome Assistant OS[00m.
 
-Use `ha` to access the Home Assistant CLI.
+   █████╗ ███████╗████████╗ ██████╗ ███████╗     ██████╗ ███╗   ██╗███████╗
+  ██╔══██╗██╔════╝╚══██╔══╝██╔═══██╗██╔════╝    ██╔═══██╗████╗  ██║██╔════╝
+  ███████║█████╗     ██║   ██║   ██║███████╗    ██║   ██║██╔██╗ ██║█████╗
+  ██╔══██║██╔══╝     ██║   ██║   ██║╚════██║    ██║   ██║██║╚██╗██║██╔══╝
+  ██║  ██║███████╗   ██║   ╚██████╔╝███████║    ╚██████╔╝██║ ╚████║███████╗
+  ╚═╝  ╚═╝╚══════╝   ╚═╝    ╚═════╝ ╚══════╝     ╚═════╝ ╚═╝  ╚═══╝╚══════╝
+
+  Welcome to Aetos One OS  —  by Aetos Tech Labs (www.aetostechlabs.com)
+
+  Use `ha` to access the Aetos One CLI.
+
diff --git a/buildroot-external/rootfs-overlay/usr/lib/systemd/system-preset/70-haos.preset b/buildroot-external/rootfs-overlay/usr/lib/systemd/system-preset/70-haos.preset
index 7d843537..e33b7f0f 100644
--- a/buildroot-external/rootfs-overlay/usr/lib/systemd/system-preset/70-haos.preset
+++ b/buildroot-external/rootfs-overlay/usr/lib/systemd/system-preset/70-haos.preset
@@ -1,3 +1,17 @@
 disable apparmor.service
 
 enable systemd-time-wait-sync.service
+
+enable aetos-hostname-early.service
+
+enable aetos-update-block.service
+
+enable aetos-recovery-ssh.service
+
+
+enable aetos-clock-guard.service
+
+enable aetos-addons-firstboot.service
+enable aetos-addons-firstboot.timer
+
+enable aetos-core-kick.service
diff --git a/buildroot-external/rootfs-overlay/usr/sbin/haos-cli b/buildroot-external/rootfs-overlay/usr/sbin/haos-cli
index 9c086459..21e23532 100755
--- a/buildroot-external/rootfs-overlay/usr/sbin/haos-cli
+++ b/buildroot-external/rootfs-overlay/usr/sbin/haos-cli
@@ -6,7 +6,7 @@
 run_shell() { exec /bin/ash -l; }
 interrupt() { echo " cancelled"; systemctl reset-failed "ha-cli@*"; run_shell; }
 emergency_shell() {
-    printf "\n\n[WARNING] Home Assistant CLI is not starting or was interrupted.\n"
+    printf "\n\n[WARNING] Aetos One CLI is not starting or was interrupted.\n"
     echo ""
     echo "This happens when start of all services takes longer than expected, or because"
     echo "of slow internet connection. Emergency console has been started, you can use"
@@ -21,7 +21,7 @@ emergency_shell() {
 
 trap interrupt INT
 
-printf "Waiting for the Home Assistant CLI to be ready..."
+printf "Waiting for the Aetos One CLI to be ready..."
 
 i=0
 while [ ! "$(docker ps -q -f name=hassio_cli)" ]; do
@@ -51,13 +51,13 @@ case $? in
     ;;
   130)
     # 130 is CLI termination with Ctrl+C (SIGINT)
-    printf "\nHome Assistant CLI has been interrupted.\n"
+    printf "\nAetos One CLI has been interrupted.\n"
     systemctl reset-failed "ha-cli@*"
     ;;
   143)
     # 143 graceful termination (SIGTERM). Most likely a proper shutdown.
     # Just sleep for a while until actual systemd shutdown gets invoked.
-    printf "\nHome Assistant CLI has been terminated.\n"
+    printf "\nAetos One CLI has been terminated.\n"
     ;;
   *)
     printf "\nHA CLI failed with error code: %s\n" "$?"
